rules:
  comment-empty-line-before:
    - always
    - except: [ first-nested ]
      ignore: [ stylelint-commands ]
  comment-no-empty: true
  comment-whitespace-inside:
    - always
    - severity: warning
